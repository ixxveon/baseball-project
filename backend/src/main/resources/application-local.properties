# 로컬 개발 전용 설정. 프로필을 명시하지 않고 켜면(application.properties의 spring.profiles.default=local)
# 자동으로 이 파일이 적용된다. 배포 등 다른 환경은 프로필을 명시적으로 지정하므로 이 파일이 적용되지 않는다.

# data.sql에 팀 공용 시드 데이터가 있어서 로컬에서는 항상 실행 (id 고정 + ON CONFLICT DO NOTHING이라 재부팅해도 중복 삽입 안 됨)
spring.sql.init.mode=always
